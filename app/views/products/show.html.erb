<h1><%= @product.name %></h1>
<p><%= @product.description %></p>
<p><%= number_to_currency(@product.price_cents / 100) %></p>
<%# <%= link_to 'Add to cart', add_to_cart_product_path(@product), method: :post, class: 'btn btn-primary' %>